import { romanize } from '../src/index.ts';
import { describe, it } from 'vitest';
describe('Debug บ้านจัดสรร', () => {
    it('should show how บ้านจัดสรร is romanized', () => {
        const result = romanize('บ้านจัดสรร');
        console.log('Result of บ้านจัดสรร:', result);
    });
});
//# sourceMappingURL=debug_banjadsan.test.js.map